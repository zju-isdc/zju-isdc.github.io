# 团队信息维护说明

建议按下面两部分维护：

1. `data/team/team-template.json`
用于记录姓名、职称、研究方向、主页和头像路径。

2. `images/team/`
用于放团队成员头像。

建议字段：

- `role`：页面上的角色名称
- `name`：姓名
- `title`：职称或职务
- `direction`：研究方向或负责工作
- `homepage`：个人主页链接，没有则留空
- `avatar`：头像相对路径

当前 `team.html` 仍是静态模板页，后续可按这个数据文件手动同步更新，也可以进一步改成自动读取。
